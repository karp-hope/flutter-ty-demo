import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_ty_demo/tabs/network.dart';

import '../helper.dart';

void main(){
  testWidgets('network widget test', (WidgetTester widgetTester) async{
    Widget testWidget = Helper.createMediaQueryWidget(Network());

  });
}